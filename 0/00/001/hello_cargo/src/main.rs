fn main() {
    println!("Hello, world!");
}

// Initialize Cargo (Build System AND Package Manager)
// > cargo new hello_cargo
// > cd hell_cargo

// Packages of code are referred to as crates

// Build:
// > cargo build

// Run (compiles and runs if not already build):
// > cargo run

// Check code to make sure it compiles:
// > cargo check

// Build for release:
// cargo build --release
